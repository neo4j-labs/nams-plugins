name: Release

on:
  workflow_run:
    workflows:
      - Build
    types:
      - completed
    branches:
      - devel
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  publish:
    name: Publish generated release branch
    runs-on: ubuntu-latest
    if: >-
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/devel') ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'devel')

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build and verify release artifacts
        run: npm run package:check

      - name: Publish dist to release branch
        shell: bash
        env:
          RELEASE_SOURCE_SHA: ${{ github.event.workflow_run.head_sha || github.sha }}
        run: |
          set -euo pipefail

          release_tree="$(mktemp -d)"
          cp -R dist/. "$release_tree"/

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          publish_release_tag() {
            git tag -f release
            git push --force origin refs/tags/release
          }

          git fetch origin release || true
          if git rev-parse --verify origin/release >/dev/null 2>&1; then
            git switch --track -c release origin/release
          else
            git switch --orphan release
          fi

          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -R "$release_tree"/. .
          git add -A

          if git diff --cached --quiet; then
            echo "Release branch is already up to date."
            publish_release_tag
            exit 0
          fi

          git commit \
            -m "chore: publish release artifacts from ${RELEASE_SOURCE_SHA}" \
            -m "Co-authored-by: Codex <codex@openai.com>"
          git push origin HEAD:release
          publish_release_tag
