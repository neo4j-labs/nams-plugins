---
description: Select the NAMS workspace for this OpenCode session.
---

Use `/nams:workspace use <workspace-id-or-name>`.

Requested selector: `$ARGUMENTS`

This command is handled by the NAMS OpenCode plugin before it reaches the model.
If this message appears in chat, the plugin did not intercept the command; verify
`.opencode/plugins/nams-hooks.js` is installed and try again.
